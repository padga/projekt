{% block title %}

{% endblock %}

{% block body %}

    {{ form_start(form, { method: attr.method|default('post'), action: attr.action }) }}
    {% if false == form.vars.valid %}
        {% include 'default/flash_messages.html.twig'%}
    {% endif %}
    {{ form_widget(form) }}
        <input type="submit" value="{{ submit_label|default(page_title|trans) }}" />
    {{ form_end(form) }}

{% endblock %}