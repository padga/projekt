{% extends 'base.html.twig' %}
{% block title %}
    {{ 'title.users_index'|trans }}
{% endblock %}

{% block body %}
<div class="navigation text-center">
    {{ knp_pagination_render(pagination) }}
</div>
{#{% if not category.tasks|length %}#}
        {#<a href="{{ url('category_delete', {id: category.id}) }}" title="{{ 'action.delete'|trans }}">#}
        {#{{ 'action.delete'|trans }}#}
        {#</a>#}
        {#{% endif %}#}
<a href="{{ url('auth_register') }}">
    <div class ="add-category-button">

        {{ 'message.add_user'|trans }}
    </div>
</a>
<table class="table table-striped">
    <thead>
    <tr>
        {#<th>{{ knp_pagination_sortable(pagination, 'label.id'|trans, 't.id') }}</th>#}
        <th>{{ knp_pagination_sortable(pagination, 'label.email'|trans, 't.email') }}</th>
        <th>{{ knp_pagination_sortable(pagination, 'label.first_name'|trans, 't.first_name') }}</th>
        {#<th>{{ knp_pagination_sortable(pagination, 'label.title'|trans, 't.title') }}</th>#}

        {#<th>{{ 'label.actions'|trans }}</th>#}
    </tr>
    </thead>
    <tbody>
    {% for users in pagination %}
        <tr>
            {#<td>{{ users.id }}</td>#}
            <td>{{ users.email }}</td>
            {#<td>{{ users.updatedAt|localizeddate }}</td>#}
            <td>{{ users.firstName }}</td>

            <td>
                <a href="{{ url('admin_view', {id: users.id}) }}" title="{{ 'action.view'|trans }}">
                    {{ 'action.view'|trans }}
                </a>
            </td>
        </tr>
    {% endfor %}
    </tbody>
</table>

<div class="navigation text-center">
    {{ knp_pagination_render(pagination) }}
</div>

{#{% else %}#}
    <p>
        {{ 'message.empty_list'|trans }}
    </p>
    {#{% endif %}#}
{% endblock %}