{% extends 'base.html.twig' %}

{% block title %}
    {{ 'title.add_transaction'|trans}}
{% endblock %}
 {% block body %}
     {% if pagination|length  %}

{% include 'default/_form.html.twig' with {
    page_title: 'title.add_transaction',
    attr: {
        method: 'post',
        action: url('transaction_add')
    },
    back_to_list_path: 'dashboard',
} %}
         {% else %}
         <h1>{{ 'message.no_tags'|trans }}</h1>
             <a class="space" href="{{ url('tag_new') }}">
                 <div class ="add-category-button">

                     {{ 'message.add_tag'|trans }}
                 </div>
             </a>
{% endif %}

{% endblock %}
